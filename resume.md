# resume.md — 次に触るときの覚書

最終更新: 2026-08-13

## いまどうなっているか

ドローンショーシミュレータ。**写真や絵から光点を拾って、そこへドローンを飛ばす。**
計算も描画もすべて C++ → WASM。作りは hanabi_emu_cpp / suisou_emu_cpp と同じ ABI・同じハーネス。

- GitHub: https://github.com/yomei-o/drone_emu_cpp （main に push 済み）
- Pages は未設定。設定すれば `wasmdist/drone_os/` で他と同じ形で出せる
- ローカル: `cd wasmdist && python3 -m http.server 8769` → http://localhost:8769/drone_os/

編隊は5つ。写真1(みゃくみゃく 439機) / 写真2(オロナミンC 1312機) / 絵(ドラえもん 694点) /
球 / 輪。ボタンで切り替え、放っておいても順に切り替わる。

## すぐ動かす

```sh
cd ~/test/drone_emu_cpp

EMSDK=~/emsdk ./build.sh drone_os
cd wasmdist && python3 -m http.server 8769

# ネイティブ自己テスト
clang++ -O2 -std=c++17 -I. -Isrc src/drone_os.cpp -o /tmp/drone
/tmp/drone 3200 /tmp/a.png 0 1     # フレーム数 出力PNG HUD 編隊(0写真1 1写真2 2絵 3球 4輪)

# 写真から光点を拾い直す
clang++ -O2 -std=c++17 -I. tools/mkpoints.cpp -o /tmp/mkpoints
/tmp/mkpoints assets/show.png  src/show_pts.h  /tmp/a.png 0.655 SHOW
/tmp/mkpoints assets/show2.png src/show2_pts.h /tmp/b.png 0.80  SHOW2
#             入力             出力ヘッダ       確認用    空の割合 シンボル名

# ふつうの絵から編隊を作る
clang++ -O2 -std=c++17 -I. tools/mkart.cpp -o /tmp/mkart
/tmp/mkart doraemon.jpg src/art_pts.h /tmp/c.png 600 ART
```

確認用 PNG（緑の十字が検出位置）を必ず見ること。ここを見ないと調整できない。

## 設計の要点（ここを崩さない）

**「あのゆったりした動き」と「全機が同時に揃う」感じは演出ではなく制約から出る。**

1. **最小ジャーク軌道** `s(τ) = 10τ³ − 15τ⁴ + 6τ⁵`（両端で速度も加速度もゼロ）。
   この軌道だと `v_max = 1.875 d/T`、`a_max = 5.7735 d/T²` と、距離と所要時間だけで上限が決まる
2. **全機同時到着** — T を「いちばん遠い機体が上限を守れる最小値」に取り、**全機で共有**する。
   近い機体は勝手にゆっくり動く。振り付けていない
3. **割り当て** — 総移動距離が短くなるよう2機ずつ交換して改善（`assign()` 内で n*60 回）。
   軌跡が交差しにくくなり、実運用と同じく衝突しにくい
4. **定点保持のゆらぎ** — ばね＋減衰＋風の外乱。実機は RTK-GPS でも常に数十cm揺れる
5. 機体性能は `VMAX=6 m/s / AMAX=3.5 m/s² / SEP=2m`。実測値を画面下に出している
   （速度は上限に張り付き、加速度には余裕がある、という実機と同じ状況が読める）

## 写真から光点を拾う — ハマった4点

全部実際にバグとして踏んだもの。同じ轍を踏まないように。

1. **青が落ちる** — 検出に輝度 `R×0.30 + G×0.59 + B×0.11` を使うと、**青の寄与が11%しかない**ので
   純青の LED を取りこぼす。人間の目に合わせた重みなので当然。→ **max(R,G,B)** で見る
2. **空の色がにじみに乗る** — 薄暮の写真だと青い空が LED のにじみに混ざり、彩度を上げると
   白い LED まで青くなる（「オロナミンCの瓶に青321機」という結果が出た）。
   → **局所背景の RGB を引いて「LED が足した分」**だけを見る。夜空でも薄暮でも同じ設定で通る
3. **隣の LED がくっつく** — しきい値を下げると連結成分がひとつの塊になる（写真1が451→244機に激減）。
   → 連結成分ではなく**極大点**を取る。最小間隔は孤立した小さい塊の大きさ(面積の25%点)から見積もる
4. **地平線の雲や観客のスマホを拾う** — 密集しているので「孤立点を捨てる」では落ちない。
   外接矩形が広がって本体が小さく配置される。
   → 点を塊にまとめ、**本体から離れた塊ごと捨てる**。
   **繋ぐ距離が狭いと絵の中の飛び石模様まで別の塊になる**（1418→550機に落ちた）ので最小間隔の6倍。
   本体の近く(外接矩形から12%以内)の小さな塊は飾りとして残す（みゃくみゃくの周りの星）

## 絵から編隊を作る（tools/mkart.cpp）

**ドローンは黒を描けない。** 光を出すものなので、黒い輪郭線は「機体がいない場所」にしかならない。
輪郭の位置には**隣接する面の色**を置く。そのために:

- 色を拾う窓から**黒い主線を除外**する（線の色を拾うと真っ黒になる）
- **背景の白も除外**する。外周から塗りつぶして背景を特定する。
  これをやらないと外側の輪郭が背景の白に引っ張られて全部白くなる。
  顔の白は輪郭線で囲まれているので背景には含まれない（北斎でも使った手）

`src/art_pts.h` が無いときはその編隊だけ無効になってビルドは通る（`__has_include`）。

## 編隊の大きさ（見切れ対策）

- 長辺を基準に揃えると、**画面が横長なので横長の絵だけ小さく見える**。
  画面の横と縦の両方に収まる倍率のうち小さいほうを使う
- 縦の中心は**カメラが向いている高さ**に合わせる。固定値だと縦長の絵が上に見切れる
- 外接矩形は**上下1%を切った範囲**で見る（外れ値ひとつで編隊が縮む）

## ABI

- `sim_set`: 0時間の進み 1光の強さ 2HUD 3自動切替
- `sim_get`: 0機数 1実測vmax 2実測amax 3最接近距離 4編隊 5所要時間T 6進捗
- `sim_action`: 0写真1 1写真2 2絵 3球 4輪 5着陸 6リセット

## 描画

- 夜空のグラデーション＋街のシルエット（手続き生成）
- 機体の光は**芯（狭く強い）・暈（色が見える）・靄（広く薄い）の3枚**＋**回折条**＋**瞬き**。
  2026-08-14 にここを作り直した（「ぼやけて見える」と言われて）。要点:
  - **きらきらは明るさではなく飽和から出る。** 芯の加算値を 1 より十分大きく（amp 58）して
    飛ばすと白い点になり、そこが「きらっ」に見える。芯を弱くすると全部が中間の明るさに
    圧縮されて、色付きのぼんやりした円盤になる（前がこれだった）
  - **靄を濃くすると隣同士が繋がって霧になる。** 1300機は画面上 7px 間隔なので効く
  - 回折条は絞りの羽根の回折。長さと明るさを瞬きで揺らすと伸び縮みして「きらきら」する
  - 瞬きは周期の違う2つの sin の積。位相は機体ごと（揃えると編隊全体が一斉に明滅する）
- **トーンマップは成分ごとにやってはいけない。** `x/(1+x)` を R,G,B 別々にかけると明るい
  ところから順に色が抜け、**球のような密な編隊が白い塊になる**（実際なった）。
  明るさ（最大成分）だけ圧縮して色の向きは保ち、`smoothstep(3,13,mx)` で芯だけ白へ寄せる
- **ガウシアンは縦横に分ける。** `exp(-(px²+py²)σ) = exp(-px²σ)·exp(-py²σ)` なので、
  画素ごとに exp を呼ぶ (2R+1)² 回が 2(2R+1) 回になる（靄 R=10 で 441→42）。
  1300機ぶんなのでここが山。おかげで**3枚＋条＋瞬きでも前より速い**（wasm 16.9→14.5ms/フレーム）
- **ネイティブ自己テストは自動切替を切ってある。** 切らないと到着3秒後に次の編隊へ動き出し、
  「球を撮ったつもりが、位置は球で色は次の編隊」という絵になる（一度これで誤読した）。
  6番目の引数で時間を早送りできる（4倍で 1300フレームあれば揃う）

## 未解決 / 今後

| # | 内容 |
|---|---|
| 1 | **機数の使い方** — いま全編隊で最大機数(1312)を使うので、点が少ない編隊では線が二重に太くなる。「余った機体は上空で待機」にすると線がシャープになる。ユーザに好みを聞いたまま未決 |
| 2 | オロナミンCの右下に雲の残りが少しある。瓶の裾と縦位置が近く、機械的にこれ以上切ると本体を削る |
| 3 | **Z(奥行き)は推定していない** — 1枚の写真からは原理的に不能。明るさ(1/d²)も試したが、
LEDの4分の1が飽和していて輝度の幅も 148〜196 しかなく使えなかった。ユーザ判断で2Dのまま。
本気でやるなら別角度の写真で三角測量か、ショーの飛行データ(.skyc/csv) |
| 4 | 衝突回避が「割り当ての工夫」だけ。実運用のように軌道の時間分離を入れるとより正確 |
| 5 | 音・花火との合成、文字の編隊、複数編隊のモーフィング |

## 素材について

`assets/` の2枚と `doraemon.jpg` は**出典・ライセンス未確認**。
公開リポジトリに入れるのは避けたほうがよいと伝えたが、ユーザ判断で全部コミットしている。
README にもその旨を明記済み。`tools/` は任意の画像で動くので差し替え可能。
